# 4.1 Routing, OpenRouter, and MCP Contracts

## Outcome

Implement Tasks 2, 3, and 4 from the reviewed usability plan in strict order. The
result must add pure consultation contracts, an independently tested OpenRouter
client, and an MCP compatibility adapter that advertises exactly three canonical
tools while continuing to accept all seven legacy names.

## Constraints and verified baseline

- This node is a leaf (`max depth/children/descendants = 0`), so no delegation is
  possible. The work remains solo and sequential.
- Only the seven files named in `NODE.md` may be changed. A parent directive
  restored the incidental `package-lock.json` diff to this branch's HEAD. The
  generated `.system/` skill directory is node seed state and remains unedited.
- `src/contracts.ts`, `src/openrouter.ts`, and their test files do not exist.
- `SmartAdvisorServer` currently imports MCP `Server`, `StdioServerTransport`,
  `CallToolRequestSchema`, `ListToolsRequestSchema`, Axios' default export, and
  `AxiosError`. Public signatures are `listTools()`, `callTool(name: string,
  args: any)`, `getHealthCheck()`, and `run()` with no arguments.
- Current behavior advertises seven aliases, requires `model` and `task`,
  normalizes whitespace in `sanitizeInput`, caches only text by
  provider/task/context, and returns text-only tool results.
- Installed declarations are MCP SDK 1.15.1 and Axios 1.x. MCP tool schemas allow
  `outputSchema`; `CallToolResult` always has `content` and may have an object
  `structuredContent`. Axios exposes three-argument `post<T>`, `AxiosError`
  response/status/code, timeout, and abort signal support.
- Context7 discovery found no callable Context7 tools. The documentation path is
  closed after zero failed calls; installed declarations and repository tests are
  the verified fallback. This blocker is in the node outbox.
- The binding prose overrides conflicting illustrative types: metadata that the
  provider does not return (including request/provider/cost/usage/classification
  or fallback metadata) must be optional and omitted, never synthesized.
- Existing system/tool-specific prompt strings must remain byte-for-byte intact.
  Raw task/context whitespace, indentation, and fences must reach the request.

## Execution plan

### 1. Task 2 — shared contracts and cache identity

1. Re-read the absent-file status, binding type block, and any newly merged
   imports immediately before editing tests. Write the actual imports/signatures
   and discrepancies to topical node memory and post the concise Task 2 preflight
   to the outbox.
2. Create `src/__tests__/contracts.test.ts` first. Cover:
   - omitted routing fields => `openrouter/auto`, `balanced`, `medium`, and the
     exact plugin ID `auto-router`;
   - Fast/Best tier mapping, explicit custom/direct routes, and direct-route
     plugin omission;
   - preservation of allowed/excluded model arrays and explicit max tokens;
   - cache-key separation by prompt version, intent, resolved model, preset,
     cost tier, restrictions, max tokens, task, and context;
   - deterministic identity without mutating input arrays.
3. Run `npm test -- --run src/__tests__/contracts.test.ts` and record the exact
   missing-module RED result before production edits.
4. Create `src/contracts.ts` with `ConsultInput`, `ConsultationResult`,
   `ConsultationReceipt`, `SmartErrorDetails`, `ResolvedRoute`, `resolveRoute`,
   and `buildConsultationCacheKey`. Use `JSON.stringify` over a fixed-order array
   of normalized route fields and unmodified task/context. Keep provider-derived
   receipt fields optional; keep locally known route, latency, and cache fields
   typed.
5. Re-run the focused command to GREEN, then run the adjacent existing server
   suites to expose integration friction without changing Task 4 yet. Record
   exact test counts/results in memory and post a Task 2 milestone.

### 2. Task 3 — OpenRouter request/response boundary

1. Re-read `contracts.ts`, the current server request builder, Axios declarations,
   and test mock shape. Record actual imports/signatures/discrepancies in memory
   and the outbox before editing `openrouter.test.ts`.
2. Create `src/__tests__/openrouter.test.ts` first. Cover:
   - missing/blank key => stable `MISSING_API_KEY` details without the key;
   - default Auto body has `openrouter/auto`, root `session_id`,
     `usage: {include: true}`, max tokens, and exactly one plugin object with
     `id`, `cost_tier`, `allowed_models`, and `excluded_models`;
   - headers include `X-OpenRouter-Metadata: enabled`; tests must not snapshot an
     authorization value;
   - a direct model omits `plugins` entirely;
   - task/context bytes and existing system prompt content survive in `messages`;
   - complete metadata normalizes to a typed receipt, while omitted provider,
     request ID, usage/cost, classification, and fallback fields stay absent;
   - 401, 402, 403/restriction, 404/no eligible model, and 429 stop after one
     attempt; timeout/408 and 503 make no more than three total attempts;
   - `Retry-After` seconds/date handling is copied to `retryAfterMs` when valid.
3. Run the focused OpenRouter command and capture missing-module RED.
4. Implement the minimum `OpenRouterClient` plus `normalizeOpenRouterError`.
   Constructor options may add timeout/retry/max-token and a prompt-builder
   callback while preserving the required `consult(input)` method. Build plugin
   data only for Auto routes. Retry only timeout/408/503, with an injected or
   zero-delay test seam so focused tests stay fast. Throw stable error data
   without normal-output stacks or request secrets.
5. Normalize only observed response values. Use the requested route for
   `requestedModel`/preset/tier, response `model` only for `selectedModel`, and
   measured latency/cache status from local facts. Never infer provider or
   fallback state from a model string.
6. Run OpenRouter GREEN, then contracts plus the existing server suites. Record
   exact counts and post the Task 3 milestone.

### 3. Task 4 — canonical MCP tools and hidden compatibility aliases

1. Re-read `SmartAdvisorServer.listTools`, `callTool`, `getHealthCheck`, `run`,
   MCP result declarations, and both server-test imports. Record actual
   signatures and the stale seven-advertised/model-required/text-only/cache-key
   discrepancies in memory and outbox before changing tests.
2. Replace stale contract expectations in the two owned server suites while
   retaining focused coverage for useful validation/health behavior. Prove RED
   for:
   - exactly `consult`, `smart_doctor`, `smart_status` in that order;
   - only `task` required and all `ConsultInput` fields represented in schema;
   - readable text plus typed `structuredContent` on successful consultation;
   - all seven hidden aliases callable and mapped as
     `code_review`/`review_code` => `code-review`,
     `expert_opinion`/`ask_expert` => `expert-opinion`, and the remaining three
     => `advice`;
   - raw whitespace/fences, route-sensitive cache identity, cached receipt age,
     and `fresh: true` read bypass;
   - safe `smart_doctor` and `smart_status` diagnostics;
   - stable local-rate and circuit-breaker failures without secrets/stacks.
3. Run both focused server files and capture the expected old-list/required-model
   RED before production edits.
4. Refactor the adapter minimally:
   - keep public method names/signatures and MCP request handlers;
   - retain existing prompt constants and `buildToolSpecificPrompt`; map canonical
     intent separately from the alias used to select the preserved prompt;
   - validate raw strings without whitespace normalization;
   - route consult/aliases through `OpenRouterClient` configured with the
     preserved prompt builder;
   - change cache entries to typed consultation results/timestamps, build keys
     with `buildConsultationCacheKey`, bypass only reads for `fresh`, and return
     cache-hit/age facts in a copied receipt;
   - advertise output schemas and return `{content, structuredContent}`;
   - handle diagnostics before consultation-only rate limiting; expose key
     presence but never its value;
   - normalize local limiter and circuit-breaker failures into stable codes and
     actions while leaving no prompt/context/authorization data in logs.
5. Run the two server suites to GREEN, then both new focused suites and the
   existing prompt suite. Record exact counts and post the Task 4 milestone.

### 4. Verification and handoff

1. Run the three required focused commands exactly as written, followed by
   `npm run build` and `git diff --check`.
2. Run the node test script required by the completion gate. Inspect the diff and
   status to verify only the seven owned project files are staged/changed relative
   to the dispatcher baseline; allow the normal Fractal lifecycle to handle the
   generated node seed state.
3. Fold memory to delivered present tense with exact RED/GREEN evidence and no
   forward-looking terminal todo. Promote only reusable routing/MCP facts to the
   project wiki, or post one outbox line explaining why no promotion is useful.
4. Drain saved radio messages, post one concise final evidence summary, commit
   only owned project files using `fractal commit`, verify branch cleanliness as
   far as node-owned files allow, and run
   `fractal node finish --reason="routing, OpenRouter, and MCP contracts complete"`
   while the node is active.

## Feasibility

The plan has roughly 17 minutes left in PLAN and 4.2784 USD in the run budget at
this checkpoint. Execution is constrained to three small boundaries and seven
owned files. Rewriting the two stale server tests around the new public contract,
rather than preserving hundreds of obsolete routing assertions, keeps the work
inside the remaining step and iteration budgets. Each production edit is gated
by one focused RED and immediate GREEN, so a budget boundary still leaves a
coherent, evidenced task boundary.

## Post-Mortem

### Accomplishments

- Verified the existing server imports and public signatures, the absent Task 2
  and Task 3 modules, MCP SDK 1.15.1 result/schema declarations, and Axios request
  and error declarations.
- Confirmed Context7 has no callable tools and reported the verified documentation
  blocker without making failed calls.
- Produced the ordered TDD implementation plan and a present-tense memory state.
- Restored the incidental parent-owned lockfile drift on explicit parent direction
  and committed the coherent planning boundary as `9d6253f`.

### Deviations

- Reserve Mode activated before implementation and explicitly prohibited starting
  new work. No owned TypeScript file changed, no focused RED/GREEN run occurred,
  and Tasks 2–4 remain wholly unimplemented.
- No project-wiki page was added because planning-only findings are private node
  state, not a reusable delivered architecture or convention.

### Continuation notes

- The parent directive to implement the smallest spec-complete Tasks 2–4 boundary
  is saved as radio message `16E02E67`.
- Resume at Task 2 preflight and strict RED; do not skip ahead to OpenRouter or MCP
  adapter changes. The exact remaining acceptance checklist is in memory/state.md.
