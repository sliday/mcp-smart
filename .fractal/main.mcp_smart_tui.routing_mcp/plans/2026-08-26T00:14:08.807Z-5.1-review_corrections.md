# 5.1 Spec-Critical Routing Review Corrections

## Outcome

Close the five parent-review blockers on the already-delivered Tasks 2–4
boundary without widening ownership or disturbing the stable public server
methods. This plan supplements, rather than replaces, the delivered 4.2 plan.

## Verified baseline

- The last commit is a normal continuation save, not a failed-step backstop.
  The only uncommitted file is the node's present-tense memory state; no owned
  TypeScript work is interrupted.
- This node is a leaf (`max depth/children/descendants = 0`), so the correction
  work is necessarily solo and sequential. There are no child branches.
- `CircuitBreaker.execute(operation)` currently calls `onFailure()` for every
  rejection. `OpenRouterClient.consult(input)` surfaces stable
  `OpenRouterError.details`, so the minimum classification seam can use those
  details and must explicitly exclude permanent failures and cancellation.
- `resolveRoute(input)` currently treats every non-`openrouter/auto` model as a
  direct custom model. The binding spec requires the compatibility alias
  `smart-auto` to resolve to the existing direct `openai/gpt-5-mini` route.
- The MCP SDK 1.15.1 declarations provide
  `InMemoryTransport.createLinkedPair()`, `Client.connect()`, and
  `Client.callTool()`. `CallToolResult` allows `structuredContent` and `isError`
  and explicitly recommends tool-originated errors be returned as an error
  result rather than a protocol-level error.
- `OpenRouterClient.send()` currently converts a missing completion to an empty
  answer and reads obsolete top-level `provider`, `task_type`, and
  `fallback_used` fields. OpenRouter's current official router-metadata page
  confirms `openrouter_metadata.endpoints.available[]` entries contain
  `provider` and boolean `selected`; the parent additionally confirmed Auto
  classification at `openrouter_metadata.pipeline[].data.task_type`.
- `integration.test.ts` snapshots only `OPENROUTER_API_KEY`, restores an absent
  value by assignment, and leaks `RATE_LIMIT_REQUESTS`.
- Context7 is absent from the callable tool registry. This blocker is already
  recorded in memory and outbox; installed MCP/Axios declarations and official
  OpenRouter documentation are the verified fallback.

## Ordered TDD execution

### 1. Circuit breaker and environment isolation

1. Re-read `SmartAdvisorServer.ts`, `openrouter.ts`, the two server tests, and
   the Axios abort declarations; record actual imports/signatures and the
   rejection-classification discrepancy in memory and outbox before test edits.
2. Add focused server coverage with circuit threshold 1 proving a first 401 (or
   another permanent stable domain failure) does not open the breaker and a
   following consultation reaches Axios. Strengthen cancellation coverage so an
   Axios `ERR_CANCELED` makes one attempt and does not poison the next server
   call.
3. Fix integration setup/teardown by snapshotting both relevant environment
   variables and restoring each with assignment only when originally present,
   otherwise deleting it.
4. Capture focused RED, then make the minimum breaker change: count only stable
   transient provider-availability failures, with cancellation represented
   distinctly and excluded. Capture focused GREEN and commit this boundary.

### 2. `smart-auto` compatibility route

1. Re-read `contracts.ts`, `openrouter.ts`, and their focused tests; record the
   exact exports/signatures and missing alias mapping before editing tests.
2. Add contract RED proving `resolveRoute({model: 'smart-auto'})` returns
   `openai/gpt-5-mini`, custom/direct routing, and no plugin. Add request RED
   proving the wire model is `openai/gpt-5-mini` and `plugins` is absent.
3. Implement only the alias resolution in `resolveRoute`, then run contracts
   GREEN, OpenRouter GREEN, and the adjacent server suites. Commit the boundary.

### 3. Stable errors across the real MCP transport

1. Re-read installed `Server`, `Client`, `InMemoryTransport`, and
   `CallToolResult` declarations plus the handler registration. Record exact
   imports/methods and the current thrown-error-to-JSON-RPC discrepancy before
   editing `integration.test.ts`.
2. Connect the internal SDK server and an SDK `Client` with a linked in-memory
   transport. Capture RED showing a domain failure currently becomes protocol
   error `-32603` and loses stable code/action.
3. Wrap only the MCP call-tool request boundary so expected domain errors return
   readable safe content, `isError: true`, and structured error details. Keep
   direct public `callTool(name, args)`, `listTools()`, and `run()` signatures
   source-compatible. Expand the consult output schema to describe both success
   and error structured shapes if required by SDK validation.
4. Capture transport GREEN, rerun both server suites, and commit the boundary.

### 4. Malformed successful provider responses

1. Re-read the response interface and normalization path, then add an
   OpenRouter RED for a 2xx body whose first choice lacks non-empty text.
2. Throw a stable `INVALID_PROVIDER_RESPONSE` result with a generic message and
   corrective action before returning or caching. Do not retry it; classify it
   for breaker accounting only if it is explicitly treated as a transient
   provider-availability failure by the boundary from step 1.
3. Capture OpenRouter GREEN and adjacent server GREEN; commit the boundary.

### 5. Documented OpenRouter metadata normalization

1. Extend the response type permissively for `openrouter_metadata` only. Add a
   documented-shape test containing multiple available endpoints, selecting the
   provider only from the entry where `selected === true`, plus a pipeline stage
   with `data.task_type`.
2. Remove normalization from obsolete top-level provider/classification/fallback
   fields. Parse only observed nested values, ignore unknown pipeline stages,
   and continue omitting every absent optional receipt field.
3. Capture focused RED/GREEN, run adjacent suites, and commit the boundary.

## Final verification and wind-down

Run the three required focused commands, the node test script, `npm run build`,
and `git diff --check`. Verify the parent-relative diff contains only the seven
owned project files plus permitted node memory/plan state. Fold exact correction
RED/GREEN counts into memory, remove the active forward-looking correction
section, unsave radio message `5EC66F74`, post one concise evidence summary, and
finish while the node is active.

## Feasibility

The implementation is five narrow corrections over an already-green 49-test
boundary. Approximately six minutes and 1.2149 USD remain after plan review,
so execution prioritizes the ordered review blockers, focused tests, and small
commits. Legacy subsystem cleanup, broad optional-input validation, and MCP
AbortSignal plumbing remain explicitly excluded. If the current iteration
cannot finish all five boundaries, land the last fully green committed boundary
and keep the saved directive plus memory state current for continuation.

## Post-Mortem

### Accomplishments

- Rebuilt the delivered Tasks 2–4 context and adopted the parent review as a
  five-boundary correction plan instead of reopening the completed design.
- Verified the installed MCP SDK 1.15.1 in-memory transport, client call-tool,
  structured output, and `isError` interfaces.
- Verified the current official OpenRouter nested provider-metadata shape and
  preserved the exact parent directive as the saved action queue.
- Committed the plan and present-tense memory handoff with a clean worktree.

### Deviations

- Reserve mode activated before EXECUTE and prohibited starting new work. No
  correction test or production edit was made, so none of the five review
  blockers is claimed complete.
- No project-wiki page was added because this pass produced only node-specific
  execution planning, not a delivered reusable architecture contract.

### Next-iteration notes

Continue from saved radio directive `5EC66F74` and this plan. Begin with the
boundary 1 production/declaration preflight and focused RED for permanent-error
circuit-breaker poisoning plus integration environment restoration. Do not skip
ahead or unsave the directive until all five correction boundaries are green.
