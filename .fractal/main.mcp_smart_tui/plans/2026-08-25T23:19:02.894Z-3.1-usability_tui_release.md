# 3.1 MCP Smart usability and TUI release

## Objective and authority

Implement `docs/superpowers/plans/2026-08-26-mcp-smart-usability-tui.md` in its
published task order. When implementation detail is ambiguous, use
`docs/superpowers/specs/2026-08-26-mcp-smart-usability-tui-design.md` as the
binding product contract. Limit production changes to the named release files
and preserve the existing prompt behavior that the spec does not replace.

## Verified starting point

- The worktree is clean at `ec02fa3`; the latest commit is not a failed-step
  backstop and there is no interrupted project diff to adopt.
- `src/index.ts` constructs `SmartAdvisorServer` at module startup and invokes
  its argument-free `run()` method. This prevents help/version from bypassing
  key validation.
- `SmartAdvisorServer` exports the class, has an argument-free constructor that
  reads `process.env.OPENROUTER_API_KEY`, and exposes public `listTools()`,
  `callTool(name, args)`, and `run()` methods.
- Existing tests advertise seven aliases, require `model`, and assert the old
  direct Axios request. Those expectations are intentionally superseded by the
  approved canonical MCP and OpenRouter contracts.
- The package currently targets Node 16, has no React/Ink dependencies, and has
  no JSX compiler option.
- Node memory and the shared project wiki contain no prior implementation
  findings. There are no child nodes to continue or merge.
- No Context7 tool, resource, or resource template is exposed in this runtime.
  Before using Ink or MCP SDK APIs, re-check the callable tool catalog and local
  MCP configuration. Do not guess an API; if Context7 remains unavailable,
  record the verified tooling blocker and constrain code to interfaces proven
  by installed type declarations and focused tests.

## TDD protocol for every owner

1. Read the production module and the imported dependency declarations before
   editing a test. Record actual imports, signatures, and discrepancies in node
   memory (and in a child's outbox for child-owned behavior).
2. Add the smallest contract test and run only its focused Vitest target to
   capture the expected RED failure.
3. Implement the minimum production behavior with `apply_patch` for hand edits.
4. Re-run the focused test to capture GREEN, then run the owner's adjacent test
   set and TypeScript build.
5. Never expose an API key, prompt, context, or authorization header in output,
   fixtures, memory, or radio messages. Mock credentials use an unmistakably
   synthetic constant only inside tests.

## Ordered execution

### 1. Parent: dispatcher boundary

Own only `package.json`, `package-lock.json`, `tsconfig.json`, `src/index.ts`,
`src/cli.ts`, and `src/__tests__/cli.test.ts` for this task.

- Verify installed package versions and current declarations before tests.
- Add dispatcher tests for default MCP selection and help without a key; run
  `npm test -- --run src/__tests__/cli.test.ts` and retain the module-missing
  RED result in memory.
- Upgrade the runtime/dependency declarations to Node 22, React 19, Ink 7, and
  `ink-testing-library`; set `jsx` to `react-jsx`.
- Implement `parseCommand` and `runCli`. Construct `SmartAdvisorServer` only in
  the no-argument MCP branch; dynamically import React/Ink TUI code only in the
  `tui` branch. Help and version must not construct a server or require a key.
  Command branches may initially use narrow placeholders until their owned
  modules merge, but must preserve the final signatures.
- Update `src/index.ts` to await `runCli(process.argv.slice(2), process.env)`
  with stable one-line normal errors.
- Capture GREEN with the focused CLI test and `npm run build`, then commit the
  exact boundary as `feat: add command dispatcher and Node 22 runtime`.

### 2. Child: routing and MCP adapter

Only after the dispatcher commit, initialize `routing_mcp` from that commit:

```bash
fractal node init routing_mcp --path=/Users/stas/Playground/mcp-smart \
  --inherit=scripts,skills --agent=codex --model=gpt-5.6-sol --effort=high \
  --max-cost=4.5 --max-iter-cost=2.0 --reserve-budget=0.5 \
  --max-iters=3 --timeout=45m --step-timeout=20m \
  --max-depth=0 --max-children=0 --max-descendants=0 --local
```

Write its `NODE.md` before launch with exclusive ownership of
`src/contracts.ts`, `src/openrouter.ts`, `src/SmartAdvisorServer.ts`,
`src/__tests__/contracts.test.ts`, `src/__tests__/openrouter.test.ts`,
`src/__tests__/SmartAdvisorServer.test.ts`, and
`src/__tests__/integration.test.ts`. Require Tasks 2–4 in order, exact focused
RED/GREEN commands, Context7 verification before API assumptions, a clean
build, `git diff --check`, current memory, and a concise outbox report listing
signatures/discrepancies and RED/GREEN evidence. Prohibit edits to parent and
sibling files. Commit the child seed with `fractal commit ... --init`, verify
the configured cap/model from the child's worktree, then start it in a separate
command.

Monitor status, spend, and outbox. When complete, inspect the three-dot diff,
child memory, and committed focused-test evidence. Re-run its focused tests and
build from committed bytes. Merge with
`git merge --no-ff -m "merge main.mcp_smart_tui.routing_mcp" ...` only after
the Auto shape, direct routes, optional receipt fields, errors/retries, cache
identity/fresh behavior, advertised tools, and seven hidden aliases agree with
the spec. Keep the child branch/worktree available.

### 3. Child: pure CLI commands, then parent wiring

Only from the merged routing state, initialize `cli_commands`:

```bash
fractal node init cli_commands --path=/Users/stas/Playground/mcp-smart \
  --inherit=scripts,skills --agent=codex --model=gpt-5.6-terra --effort=high \
  --max-cost=2.5 --max-iter-cost=1.25 --reserve-budget=0.3 \
  --max-iters=2 --timeout=30m --step-timeout=15m \
  --max-depth=0 --max-children=0 --max-descendants=0 --local
```

Its `NODE.md` exclusively owns `src/commands/` and
`src/__tests__/commands.test.ts`. Require Task 5 TDD, pure return values, no
server/TUI wiring, JSON-safe errors without stacks, missing-key doctor behavior,
multiline task preservation, focused RED/GREEN evidence, build, diff check,
memory, and outbox. Merge only after review and focused verification; preserve
the branch/worktree.

The parent then reads the merged command signatures and patches only
parent-owned `src/cli.ts` and `src/__tests__/cli.test.ts`. Add focused RED tests
for text/JSON formatting, `ask` whitespace preservation, version, unknown
commands, and no-key behavior before wiring the pure functions. Capture focused
GREEN and build evidence in parent memory.

### 4. Child: Ink TUI

Only from the merged command state and parent wiring commit, initialize
`ink_tui`:

```bash
fractal node init ink_tui --path=/Users/stas/Playground/mcp-smart \
  --inherit=scripts,skills --agent=codex --model=gpt-5.6-sol --effort=high \
  --max-cost=4.5 --max-iter-cost=2.0 --reserve-budget=0.5 \
  --max-iters=3 --timeout=45m --step-timeout=20m \
  --max-depth=0 --max-children=0 --max-descendants=0 --local
```

Its `NODE.md` exclusively owns `src/tui/` and
`src/__tests__/tui.test.tsx`. Require Task 6 TDD and explicit tests for the
keyboard map, visible focus/route/stage/error action/receipt, below-80-column
layout, byte-preserving multiline input, `alternateScreen: true`,
`exitOnCtrlC: false`, first Ctrl+C cancellation and second Ctrl+C exit, optional
receipt fields, and labeled partial Compare success/failure. Require focused
RED/GREEN evidence, build, diff check, memory, and outbox. Merge only after
reviewing those behaviors and re-running focused tests from committed bytes;
preserve the branch/worktree.

After merge, the parent adds a CLI test proving that only the `tui` branch
dynamically imports the TUI and verifies the real `runTui` interface.

### 5. Parent: documentation and final integration

- Rewrite `README.md` around Node 22, environment-only setup, doctor, TUI,
  one-shot ask, canonical stdio MCP configuration, Auto presets, typed receipts,
  and legacy aliases, using copyable commands and no fabricated calls.
- Run focused suites after every integration, then
  `npm test -- --run`, `npm run build`, `git diff --check`, and the node's
  absolute `scripts/test.sh` gate.
- Smoke-test built help, version, missing-key doctor JSON, and no-argument stdio
  startup without allowing it to hang. Validate JSON mechanically.
- Review every acceptance criterion in the binding spec, scan tracked content
  for secrets without printing matched values, and confirm forbidden files are
  unchanged.
- Fold parent and child evidence into topical memory pages and a terminal spec
  checklist. Promote only durable architecture/compatibility facts to the
  project wiki, lint both stores, commit all project work, and finish only from
  a clean worktree with all child branches retained.

## Budget and feasibility

Current subtree budget remaining is 19.8144. Child run caps total 11.5
(`routing_mcp` 4.5, `cli_commands` 2.5, `ink_tui` 4.5), leaving at least 8.3144
for parent Task 1, integration, documentation, and wind-down before soft-cap
overshoot. Children run sequentially because each requires the previous merged
baseline; no speculative parallel spawn is valid. Their per-iteration caps and
reserve allocations allow both implementation and wind-down. If actual child
spend approaches its cap, stop new scope, demand committed evidence, and either
merge a verified deliverable or explicitly absorb only unfinished integration
work owned by the parent.

The remaining wall-clock budget is about 118 minutes. The release is feasible
only if Task 1 is completed and the first child is launched immediately in
EXECUTE, children are actively monitored, and no work outside the approved
feature is undertaken.

## Post-Mortem

### Accomplishments

- Verified the existing constructor and public server signatures before adding
  tests.
- Captured the expected missing-module RED, then landed three GREEN dispatcher
  tests and a successful TypeScript build.
- Committed the Node 22/lazy dispatcher boundary as `ff7d30a` and verified the
  adjacent 50-test baseline.
- Configured and launched `routing_mcp` from that exact commit with a local
  branch, inherited scripts, exclusive file ownership, focused TDD gates, and a
  verified 4.5 cap.
- Reviewed the committed diff and aligned `@types/node` with the Node 22 runtime
  floor; the focused tests and build remain GREEN.

### Deviations

- Context7 is not exposed in the callable tool catalog, MCP resources, or MCP
  templates. No dependency API was guessed in the dispatcher work; the routing
  child has the same stop/report requirement before it makes API assumptions.
- Task 1 initially retained Node 20 declarations. REVIEW caught and corrected
  the mismatch before the iteration commit.
- The existing full server suite passes but emits verbose logger output. Clean
  final test output remains a release gate for the routing/MCP integration.
- Child setup normalized a parent-owned lockfile entry and Codex injected
  untracked system skills into the child seed. The parent directed the child to
  restore the lockfile and leave seed capture to normal Fractal commit behavior.

### Next-iteration notes

- Treat `routing_mcp` as the primary work: monitor spend and outbox, inspect its
  committed three-dot diff and evidence, and independently re-run focused tests
  before merging.
- Do not initialize `cli_commands` until the routing child has been reviewed and
  merged. Preserve the routing branch/worktree after integration.
- Keep the saved dirty-state radio item open until the child confirms its
  project ownership boundary is clean or reports a commit scope failure.
